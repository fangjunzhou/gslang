[project]
name = "gslang"
version = "0.1.0"
description = "3D Gaussian Splatting with BVH optimization in Slang"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.5",
    "slangpy>=0.23.0",
    "matplotlib>=3.10.1",
    "numpy-quaternion>=2024.0.8",
    "pyglm>=2.8.2",
    "jax>=0.6.0",
    "pyntcloud>=0.3.1",
    "pillow>=11.2.1",
    "pycolmap>=3.11.1",
    "pyvista[full,jupyter]>=0.45.2",
    "tqdm>=4.67.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "black>=25.1.0",
    "ipywidgets>=8.1.7",
    "jupyterlab>=4.4.1",
    "pyright>=1.1.400",
    "pytest>=8.3.5",
    "pytest-benchmark[histogram]>=5.1.0",
]

[tool.uv.sources]
slangpy = { path = "external/slangpy" }

[tool.black]
line-length = 80

[tool.pyright]
include = ["./src"]
pythonVersion = "3.12"
typeCheckingMode = "basic"
reportMissingSuperCall = "warning"
reportInvalidStringEscapeSequence = "error"
reportMissingParameterType = "warning"
reportMissingTypeArgument = "warning"
reportInvalidTypeForm = "warning"
